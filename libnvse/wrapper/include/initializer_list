#pragma once
#include "cstddef"
namespace std {
    template<typename T>
    class initializer_list {
    public:
        typedef T value_type;
        size_t size() const;
        const T* begin() const;
        const T* end() const;
    };
}
