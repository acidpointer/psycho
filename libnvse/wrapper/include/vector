#pragma once
#include "cstddef"
namespace std {
    template<typename T>
    class vector {
    public:
        typedef T value_type;
        size_t size() const;
        T& operator[](size_t);
        const T& operator[](size_t) const;
    };
}
